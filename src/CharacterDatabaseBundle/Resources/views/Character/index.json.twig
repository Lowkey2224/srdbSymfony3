[
    {% for char in characters %}{
        "id": "{{ char.id }}",
        "name": "{{ char.name }}",
        "user": {
            "name": "{{ char.user.username }}",
            "id": "{{ char.user.id }}"
        }
    }{% if not loop.last %},{% endif %}
    {% endfor %}
]